name: Flutter CI with Firebase

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - uses: actions/checkout@v3

      # 2️⃣ Setup Java (needed for Gradle)
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3️⃣ Setup Flutter
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.8"  # Fixed version number

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # 5️⃣ Add google-services.json (Firebase config) - FIXED
      - name: Create google-services.json
        run: |
          mkdir -p android/app
          cat > android/app/google-services.json << 'EOF'
          ${{ secrets.GOOGLE_SERVICES_JSON }}
          EOF

      # 6️⃣ Verify and debug the JSON file
      - name: Debug google-services.json
        run: |
          echo "=== File exists check ==="
          ls -la android/app/google-services.json
          echo ""
          echo "=== File content ==="
          cat android/app/google-services.json
          echo ""
          echo "=== JSON validation ==="
          python3 -m json.tool android/app/google-services.json > /dev/null && echo "✅ JSON is valid" || echo "❌ JSON is invalid"
          echo ""
          echo "=== File encoding check ==="
          file android/app/google-services.json

      # 7️⃣ Install Firebase CLI
      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      # 8️⃣ Authenticate Firebase
      - name: Firebase login
        run: firebase use --add deployment-8eb89 --token "${{ secrets.FIREBASE_TOKEN }}"

      # 9️⃣ Run tests
      - name: Run tests
        run: flutter test || true

      # 🔟 Clean before build
      - name: Clean Flutter
        run: flutter clean && flutter pub get

      # 1️⃣1️⃣ Build release APK
      - name: Build APK
        run: flutter build apk --release

      # 1️⃣2️⃣ Upload APK to artifacts
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release.apk
          path: build/app/outputs/flutter-apk/app-release.apk

